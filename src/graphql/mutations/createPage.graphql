mutation createPage($pageTitle: String!, $pageText: String!, $aboutUs: String!, $pageimgUrl: String){
    createPage(pageTitle: $pageTitle, pageText: $pageText ,aboutUs: $aboutUs, pageimgUrl: $pageimgUrl ){
        id
        pageTitle
        pageText
        pageimgUrl
        aboutUs
    }
}