mutation EditPage($id: Int!, $pageTitle: String!, $aboutUs:String!, $pageText:String!, $pageimgUrl: String){
  editPage(id: $id, pageTitle:$pageTitle, aboutUs:$aboutUs, pageText:$pageText, pageimgUrl: $pageimgUrl){
    id
    pageTitle
    pageText
    aboutUs
    pageimgUrl
    creatorId
  }
}