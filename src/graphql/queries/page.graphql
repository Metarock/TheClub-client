query Page ($id: String!){
  page(id: $id) {
    id
    pageTitle
    pageText
    pageimgUrl
    aboutUs
    creatorId
    creator {
      id
      clubName
      clubUsername
    }
    createdAt
    updatedAt
  }
}