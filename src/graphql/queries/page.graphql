query Page ($id: Int!){
  page(id: $id) {
    id
    pageTitle
    pageText
    pageimgUrl
    aboutUs
    creatorId
    creator {
      id
      clubName
      clubUsername
    }
    createdAt
    updatedAt
  }
}