query Page ($id: Int!){
  page(id: $id) {
    id
    pageTitle
    pageText
    pageimgUrl
    aboutUs
    creatorId
    creator {
      id
      clubName
      clubUsername
      email
      university
    }
    createdAt
    updatedAt
  }
}